/**
 * 调用这个方法来对boatgal.status.renderObj进行渲染
 * @method render
 * @param renderObj 就是boatgal.status.renderObj
 * @param stage 进行渲染的地方,就是boatgal.stage
 */
galRenderer.render = function(renderObj, stage) {

};