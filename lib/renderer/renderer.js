/**
 * 渲染器专门用来绘制boatgal.status.stage的内容,包括声音。
 * 渲染器通过boatgal.renderer(stage)来调用,
 * 渲染器的组件作为boatgal.renderer的属性来添加
 * @method renderer
 * @param stage 要渲染的舞台,即boatgal.status.stage
 */
boatgal.renderer = function (stage) {

};